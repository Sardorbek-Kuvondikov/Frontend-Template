// Button Click Status
function buttonClickFn(num) {
  num = Number(num);

  const btn = document.querySelector(
    `button[onclick="buttonClickFn('${num}')"]`
  );
  const copiedSpan = btn.querySelector(".js-btn-copy");
  const buttonHTML = btn.outerHTML;

  if (num === 1) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 2) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 3) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 4) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 5) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 6) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 7) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 8) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 9) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 10) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 11) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 12) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 13) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 14) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 15) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 16) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 17) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 18) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 19) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 20) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 21) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 22) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 23) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 24) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 25) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 26) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 27) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 28) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 29) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 30) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 31) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 32) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 33) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 34) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 35) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 36) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 37) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 38) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 39) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 40) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 41) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 42) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 43) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 44) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 45) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 46) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 47) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 48) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 49) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 50) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 51) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 52) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 53) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 54) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 55) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 56) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 57) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 58) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 59) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 60) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 61) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 62) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 63) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 64) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 65) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 66) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 67) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 68) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 69) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 70) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 71) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 72) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 73) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 74) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 75) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 76) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 77) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 78) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 79) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 80) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 81) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 82) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 83) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 84) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 85) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 86) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 87) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 88) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 89) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 90) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 91) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 92) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 93) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 94) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 95) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 96) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 97) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 98) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 99) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 100) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 101) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 102) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 103) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 104) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 105) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 106) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 107) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 107) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  // Buttons Bootstrap
  if (num === 108) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 109) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 110) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 111) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 112) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 113) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 114) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 115) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 116) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 117) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 118) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 119) {
    navigator.clipboard
      .writeText(
        `<input
          class="btn btn-primary"
          type="button"
          value="Input"
          />`
      )
      .then(
        () => {
          copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
          setTimeout(() => {
            copiedSpan.innerHTML = `Copy`;
          }, 2000);
        },
        (err) => {
          alert("Failed to copy code: " + err);
        }
      );
  }
  if (num === 120) {
    navigator.clipboard
      .writeText(
        `<input
          class="btn btn-primary"
          type="submit"
          value="Submit"
        />`
      )
      .then(
        () => {
          copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
          setTimeout(() => {
            copiedSpan.innerHTML = `Copy`;
          }, 2000);
        },
        (err) => {
          alert("Failed to copy code: " + err);
        }
      );
  }
  if (num === 121) {
    navigator.clipboard
      .writeText(
        `<input
          class="btn btn-primary"
          type="reset"
          value="Reset"
        />`
      )
      .then(
        () => {
          copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
          setTimeout(() => {
            copiedSpan.innerHTML = `Copy`;
          }, 2000);
        },
        (err) => {
          alert("Failed to copy code: " + err);
        }
      );
  }
  if (num === 122) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 123) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 124) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 125) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 126) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 127) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 128) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 129) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 130) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 131) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 132) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 133) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 134) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 135) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 136) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 137) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 138) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 139) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 140) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 141) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 142) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 143) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 144) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 145) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 146) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 147) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 148) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 149) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 150) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 151) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 152) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 153) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 154) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 155) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 156) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 157) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 158) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 159) {
    const dropdown = `<div class="btn-group">
  <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
    Action
  </button>
  <ul class="dropdown-menu">
    <li><a class="dropdown-item" href="#">Action</a></li>
    <li><a class="dropdown-item" href="#">Another action</a></li>
    <li><a class="dropdown-item" href="#">Something else here</a></li>
    <li><hr class="dropdown-divider"></li>
    <li><a class="dropdown-item" href="#">Separated link</a></li>
  </ul>
</div>`;
    navigator.clipboard.writeText(dropdown).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 160) {
    const dropdown = `<div class="btn-group">
  <button type="button" class="btn btn-primary">Action</button>
  <button type="button" class="btn btn-primary dropdown-toggle dropdown-toggle-split" data-bs-toggle="dropdown" aria-expanded="false">
    <span class="visually-hidden">Toggle Dropdown</span>
  </button>
  <ul class="dropdown-menu">
    <li><a class="dropdown-item" href="#">Action</a></li>
    <li><a class="dropdown-item" href="#">Another action</a></li>
    <li><a class="dropdown-item" href="#">Something else here</a></li>
    <li><hr class="dropdown-divider"></li>
    <li><a class="dropdown-item" href="#">Separated link</a></li>
  </ul>
</div>`;
    navigator.clipboard.writeText(dropdown).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 161) {
    const dropdown = `<button class="btn dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
            Dropdown
          </button>
          <ul class="dropdown-menu dropdown-menu-dark">
            <li><a class="dropdown-item" href="#">Action</a></li>
            <li><a class="dropdown-item" href="#">Another action</a></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>`;
    navigator.clipboard.writeText(dropdown).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 162) {
    const dropdown = `<button
                          class="btn btn-primary"
                          type="button"
                          data-bs-toggle="collapse"
                          data-bs-target="#collapseExample"
                          aria-expanded="false"
                          aria-controls="collapseExample"
                        >
                          Button collapse
                          <span class="ml-2"
                          ><i class="fa-solid fa-caret-down"></i
                        ></span>
                        </button>
                        <div class="collapse" id="collapseExample">
                          <div class="card card-body mt-2">
                            Some placeholder content for the collapse component.
                          </div>
                        </div>`;
    navigator.clipboard.writeText(dropdown).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 163) {
    const dropdown = `<button
                          class="btn border"
                          type="button"
                          data-bs-toggle="collapse"
                          data-bs-target="#collapseExample"
                          aria-expanded="false"
                          aria-controls="collapseExample"
                        >
                          Button collapse
                          <span class="ml-2"
                          ><i class="fa-solid fa-caret-down"></i
                        ></span>
                        </button>
                        <div class="collapse" id="collapseExample">
                          <div class="card card-body mt-2">
                            Some placeholder content for the collapse component.
                          </div>
                        </div>`;
    navigator.clipboard.writeText(dropdown).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 164) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 165) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 166) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 167) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 168) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 169) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 170) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 171) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 172) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 173) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 174) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 175) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 176) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 177) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 178) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
  if (num === 179) {
    navigator.clipboard.writeText(buttonHTML).then(
      () => {
        copiedSpan.innerHTML = `<i class="fa-solid fa-check"></i> Copied!`;
        setTimeout(() => {
          copiedSpan.innerHTML = `Copy`;
        }, 2000);
      },
      (err) => {
        alert("Failed to copy code: " + err);
      }
    );
  }
}

// Show Code
document.querySelectorAll(".js-show-code").forEach((btn) => {
  btn.addEventListener("click", () => {
    const codeElement = btn.closest("ul");
    if (codeElement) {
      const nextElemet = codeElement.nextElementSibling;
      nextElemet.classList.toggle("tw-hidden");
    }
  });
});
